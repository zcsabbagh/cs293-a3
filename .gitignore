# Large data files (>100MB, exceed GitHub limit)
# Download from: https://huggingface.co/datasets/allenai/mathfish
# Or get from shared team files
mathfish_train.jsonl
*.xlsx
.env

# its fine to push each of our annotator files, this is ideal actually

# Large generated A3 artifact (reproducible; avoid GitHub >100MB limit)
a3/krish/results/descriptive/preds_full_with_metadata.json
